#! /usr/bin/env python
# -*- coding: utf-8 -*-

# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

def binaire(a) : return (1 if a >= 0 else -1)

# ==============================================================================

# {{{ ternaire

def ternaire(a, seuil = 0) :
	if a > seuil : return 1
	if a < -seuil : return -1
	if a >= -seuil and a <= seuil : return 0

# }}}

# ==============================================================================

# {{{ floue

def floue(a, seuil) :
	y = a / float(seuil)
	return max(min(y, 1), -1)

# }}}

# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

# {{{ non

def non(a, logique = binaire) : return (1 if logique(a) == -1 else -1)

# }}}

# ========================================================================

# {{{ et

def et(a, b, logique = binaire) :
	return (1 if logique(a) == 1 and logique(b) == 1 else -1)

# }}}

# ========================================================================

# {{{ ou

def ou(a, b, logique = binaire) :
	return (1 if logique(a) == 1 or logique(b) == 1 else -1)

# }}}

# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
# %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

# {{{ teste

def teste() :
	L = []
	N = int(1e7)
	M = N // 10
	for i in range(N) :
		a = float(i) / 1000
		L = binaire(a)
		if i % M == 0 : print i, a, L

# }}}

# ========================================================================

# {{{ main

if __name__ == '__main__' :
	"""
	Logique floue
	1        : Vrai
	-1 --> 1 : Ambigu
	-1       : Faux
	"""
	pass

# }}}
